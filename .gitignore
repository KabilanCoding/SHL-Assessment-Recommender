# Environment / secrets — NEVER commit these
.env
*.env.local

# Python cache
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/

# Virtual environments
venv/
.venv/
env/

# Data files — old local model embeddings excluded, gemini cache committed
data/embeddings.npy
data/embeddings_v2.npy
# data/embeddings_gemini.npy  ← committed so Render skips the API call on startup

# The catalog JSON is downloaded automatically — optional to include
# data/shl_catalog.json

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
